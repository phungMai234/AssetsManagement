import styled from 'styled-components';

const Wrapper = styled.div`
  .header-content {
    display: flex;
    align-items: center;

    span {
      padding-left: 10px;
    }
  }
  .btn-sm {
    font-size: 16px;
  }
`;
export default Wrapper;
